# ADR 0002: Drizzle ORM

Accepted. Drizzle keeps schema and queries strongly typed while producing inspectable SQL migrations. Database constraints remain authoritative and domain services remain independent from ORM objects.
